# Base URL absoluta da API (sem barra no final). Usado em produção (Vercel) para
# falar com o servidor publicado no Railway. Em dev, deixe vazio para usar o
# proxy local /api.
# Exemplo: VITE_API_BASE_URL=https://api.dnmaria.com.br
VITE_API_BASE_URL=
